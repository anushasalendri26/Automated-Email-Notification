# Automated-Email-Notification
Developed a Python-based Automated Email Notification System that reads customer email addresses from a file and sends personalized confirmation emails automatically.  The project uses smtplib and email.mime to send emails and python-dotenv to securely manage email credentials and app passwords through environment variables instead of hardcoding
